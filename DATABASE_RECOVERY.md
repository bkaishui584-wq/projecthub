# ProjectHub 数据库恢复说明

## 恢复原则

- 恢复前不要覆盖唯一一份生产备份。
- 先在隔离数据库恢复并验证，再切换生产连接。
- 任何恢复都必须记录时间、操作人、备份版本和验证结果。
- 不得通过删除数据目录来“修复”损坏，除非已确认没有任何有效数据。

## 推荐备份策略

- PostgreSQL：每天自动全量备份，至少保留 7 个版本，并保留一份异地备份。
- 文件：PostgreSQL 中的 `projecthub_file_blobs` 随数据库一起备份。
- 配置：只备份变量名和环境配置清单，不把密钥写入普通文档或 Git。
- 旧 JSON：迁移完成后至少保留 30 天，再根据制度归档。

## PostgreSQL 恢复流程

1. 创建恢复用空数据库。
2. 把最近的备份恢复到恢复库。
3. 配置一组临时环境变量，把服务连接到恢复库。
4. 启动服务但不开放公网写入。
5. 检查用户、项目、消息、申请、通知、文件数量和抽样内容。
6. 执行登录、发消息、下载文件、管理员删除项目等冒烟测试。
7. 验证通过后切换生产 `DATABASE_URL`。
8. 恢复完成后创建新的即时备份。

## JSON 回滚恢复

```text
STORAGE_DRIVER=file
ALLOW_EPHEMERAL_STORAGE=1
DATA_DIR=<旧 data 目录的绝对路径>
```

只在明确接受临时文件存储风险时使用，并立即另做数据库迁移。

## 故障处理

| 故障 | 处理 |
| --- | --- |
| 数据文件损坏 | 保留损坏文件，不静默清空；从最近快照恢复 |
| PostgreSQL 不可用 | 服务健康检查返回 503，禁止继续接受写入 |
| 保存失败 | API 返回服务器错误，写入不会假装成功 |
| 上传文件缺失 | 下载返回 404，创建故障记录并从备份恢复 |
| 错误重部署 | 回滚到上一版本镜像/配置，不删除数据库 |
