{
  "name": "projecthub",
  "version": "1.0.0",
  "private": true,
  "description": "ProjectHub —— 面向大学生的线上项目协作社区",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "db:check": "node scripts/db-check.js",
    "db:migrate": "node scripts/db-migrate.js",
    "db:import-local": "node scripts/db-import-local.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
