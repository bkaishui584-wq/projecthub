# ProjectHub V2 Phase 0 完整项目审计

- 审计日期：2026-09-17
- 审计对象：`C:\Users\YUNIAN\Documents\ChatGPT\项目\projecthub-site`
- Git 分支 / 基线：`security-hardening` / `e261899`
- 审计范围：身份认证、授权、Session、管理员、项目/申请/成员、聊天、SSE、文件、通知、AI、数据持久化、Render/Docker、前端安全与移动端结构
- 本阶段性质：只读审计。未修改业务代码、未修改 `data/store.json`、未迁移数据库、未删除任何用户数据。
- 当前工作树提示：`Dockerfile` 有未提交修改，`package-lock.json` 未跟踪；修复阶段开始前需要纳入版本管理。

> 本报告不记录密码、Token、API Key 或管理员密钥。历史泄露凭据只按“已泄露”处理。

## 1. 执行摘要

ProjectHub 当前是一个架构简单、零运行时 npm 依赖的原生 Node.js 单体应用。它已经具备一批基础安全措施，包括 HttpOnly Cookie、CSRF 双提交校验、SSE 短期票据、密码加盐哈希、请求体/JSON 限额、限流、并发闸门、文件魔数检查和基础安全响应头。

但当前版本尚不满足正式生产标准，核心原因不是某一个单独漏洞，而是以下四类系统性问题：

1. 数据和 Web Service 生命周期绑定在 Render 临时文件系统上，重启、休眠后重新部署或实例更换可能造成整站数据丢失；备份也在同一数据目录内，不能提供异地恢复能力。
2. 话题公开视图会对浅拷贝的 `ai` 字段原样返回，可能把私密话题的方案草稿、投票选项、公告和深度分工建议泄露给未登录用户。
3. 撤回权限与最终验收目标不一致。当前负责人/管理员可以撤回其他成员的消息，普通用户的消息完整性无法保证。
4. AI 层已有超时和基本清理，但缺少严格结构校验、有限重试、Prompt 版本管理、评测集、按用户/全站的调用预算和完整的状态机约束。

因此 Phase 1 应先解决数据持久化与可恢复性，再处理权限和 AI 数据泄露，最后才会进入 UI、通知和全面上线验收。

## 2. 当前系统架构

```text
浏览器
  ├─ index.html + styles.css + script.js
  ├─ HttpOnly Cookie + CSRF Cookie
  ├─ JSON API
  └─ SSE /api/events（一次性 ticket）
        │
        ▼
Node.js 原生 http.createServer
  ├─ 静态文件白名单
  ├─ 注册 / 登录 / 退出 / 当前用户
  ├─ 话题 / 申请 / 成员 / 标签 / 举报
  ├─ 聊天消息 / 文件上传下载
  ├─ 通知中心 / SSE 推送
  ├─ 管理员用户与项目操作
  ├─ ai.js：本地模型探测 / DeepSeek / OpenAI 兼容接口
  └─ security.js：限额、限流、并发、SSE、安全头、审计日志
        │
        ▼
data/
  ├─ store.json       用户、Session、话题、消息、申请、通知等
  ├─ store.json.bak   上一次写入版本
  ├─ backups/         最长 7 天的同目录快照
  └─ files/           上传的 Word / jpg / png
```

### 技术栈

- 后端：Node.js 原生 `http`、`fs`、`path`、`crypto`、`fetch`。
- 前端：原生 HTML/CSS/JavaScript，无 React/Vue。
- 数据库：当前没有数据库，使用单文件 JSON 存储。
- ORM：无。
- 实时通信：SSE，不是 WebSocket。
- AI：`ai.js` 自动探测本机兼容模型，或读取 DeepSeek、OpenAI、任意 OpenAI 兼容服务环境变量。
- 部署：Render Node Web Service；Dockerfile 和 Docker Compose 作为替代部署方式。
- 依赖：`package.json` 没有运行时依赖；`package-lock.json` 当前未跟踪。

## 3. 数据模型与数据流

`data/store.json` 顶层结构：

```text
users           按用户 ID 索引
sessions        token -> userId / createdAt / lastSeenAt / expiresAt
topics          话题数组；成员内嵌在 members
messages        topicId -> 消息数组
applications    申请数组
files           topicId -> 文件元数据数组
notifications   userId -> 通知数组
reports         举报数组
```

关键数据流：

- 注册：昵称/年级/方向 -> 密码 scrypt 加盐哈希 -> 创建用户 -> 创建 Session -> 写入 `store.json`。
- 登录：昵称 -> 查用户 -> scrypt 校验 -> 创建 Session -> HttpOnly Cookie + CSRF Cookie。
- 创建话题：后端生成项目 ID 和项目编号 -> 负责人进入 `members` -> 写入 `topics`、`messages`、`files`。
- 申请加入：后端从当前登录账号确定申请人 -> 校验成员状态、人数上限、重复申请、公开方向或私密密码 -> 写入 `applications` -> 通知负责人。
- 聊天：成员校验 -> 生成消息 -> 关联 @ 和引用 -> 推送 SSE/通知 -> 写入 JSON。
- 文件：成员校验 -> base64 JSON 请求 -> 大小/魔数/配额校验 -> 写入 `data/files/` -> 元数据写入 `store.json`。
- AI：负责人开启 -> 点击“开始思考” -> 读取最近聊天 -> 生成草稿和方向 -> 成员匿名投票 -> 公告 -> 三轮后可深度分工。
- 管理员：角色 `admin` -> 用户列表、封禁/解封、查看举报、删除任意话题、访问话题内部接口。

## 4. 权限模型

### 当前角色

```text
admin / 管理员
  拥有普通用户能力和全局管理能力
  可封禁/解封非管理员账号
  可删除任意话题
  可作为话题 owner 处理话题数据和 AI 操作

owner / 项目负责人
  话题 creatorId === 当前用户 ID
  可处理申请、移出成员、设置标签、删除项目、控制 AI
  当前也可撤回其他成员消息（需要修复）

member / 成员
  可读取消息和文件、发送消息、上传文件、投票、撤回自己的消息
  不能审批申请、移出成员、设置标签或删除项目

guest / 游客
  可读取公开项目列表
  不能加入、聊天、下载文件、访问管理员接口
  私密话题只能看到有限信息，但也存在 AI 字段泄露问题
```

### 授权结论

- 大部分写接口在服务端重新读取 `authUser(req)`，没有只依赖前端按钮。
- `projectId`、`messageId`、`applicationId`、`fileId` 多数会再查服务端资源并做成员/负责人校验。
- 发现的最直接越权问题是消息撤回：负责人/管理员路径允许撤回他人消息。
- 发现的最严重对象级泄露是话题列表的 `ai` 字段未按查看者做投影。
- 管理员接口有独立角色校验；普通用户伪造 URL 不能直接进入管理员接口。
- 封禁会删除该用户 Session，并阻止发言、上传、创建项目和申请；但封禁前已存在的文件/消息不会被清理，这是产品策略问题，不是直接越权。

## 5. 聊天、实时同步与通知

- 聊天消息使用服务端生成 ID、`author`、`authorName`、`text`、`replyTo`、`mentions`、`recalled`、`time`、`at`。
- HTTP 写入后通过 SSE 向话题成员推送；客户端也会轮询刷新。
- 引用只在消息发送时复制作者名和最多 80 字文本，避免引用对象被后续删除影响。
- @ 仅根据成员昵称匹配，发送后写入 `mentions` 并产生通知。
- SSE 使用一次性 ticket、连接总数/单 IP 上限和空闲回收，不把 Session token 放到 URL。
- 通知与聊天消息分开保存，但通知容量上限为每用户 200 条。
- 当前聊天时间显示使用服务端生成的 `time` 字符串，而不是使用标准 `at` 时间戳在浏览器本地格式化，因此 Render UTC 环境下会出现时间偏差。
- 当前消息撤回服务端逻辑为作者本人或 owner/admin；与“用户 A 无法撤回用户 B 消息”的验收标准冲突。
- `scheduleSave()` 只延迟 300ms 落盘，且计时器被 `unref()`；消息和通知发生后的短时间窗口内进程退出可能丢失最近变更。

## 6. 项目申请系统

已实现：

- 必须登录，封禁账号不能申请。
- 已经加入项目不能重复申请。
- 同一用户对同一项目只拦截 `pending` 状态，拒绝后可重新申请。
- 人数达到上限时拒绝。
- 私密话题必须校验 6 位密码。
- 公开话题必须至少有方向匹配。
- 负责人才能查看和处理申请。
- 处理申请时再次检查人数上限、申请状态和申请人账号。
- 批准后写入成员并通知申请人；拒绝后通知申请人。
- `my-applications` 只返回当前登录用户自己的申请。

仍需改善：

- 没有取消申请接口。
- 没有按项目/按用户的申请频率限制，拒绝后可反复申请造成通知骚扰。
- 没有统一状态枚举和服务端状态迁移表。
- 私密密码验证没有独立的失败锁定和专门的暴力破解防护。
- 管理员没有独立的申请管理视图。

## 7. 时间系统

- 数据对象主要存储 `Date.now()` 毫秒时间戳，适合统一按 UTC/epoch 处理。
- 消息额外存储服务端本地字符串 `time`，当前前端直接显示该字符串。
- 前端 `formatDate()` 使用浏览器本地时区，申请和通知时间通常正确。
- 因此同一页面会出现两种时间语义：申请/通知按浏览器时区，聊天消息按服务器时区。
- 未发现固定加减小时的代码，但消息 `time` 字段是根因。
- Phase 4 应保留 `at`/ISO 时间，停止依赖服务端 `time` 字符串，并由客户端统一格式化。

## 8. 移动端结构现状

- 已设置 viewport。
- 已有 `max-width: 960px` 和 `max-width: 640px` 媒体查询。
- 聊天使用 `height: calc(100vh - var(--topbar-h))`，并设置 `min-height: 520px/560px`。
- 侧栏有移动端滑入样式，但尚未进行 320/375/390/430/768px 的实际浏览器回归。
- `100vh` 在移动浏览器地址栏和软键盘出现时不稳定，短屏设备可能造成输入框被遮挡或页面整体溢出。
- 当前 CSS 没有系统性的 `dvh`、safe-area、键盘可视区域处理。
- Phase 4 需要真正进行响应式重构和浏览器尺寸回归，不能只依赖现有媒体查询。

## 9. AI 系统

### 当前配置结论

- 本次审计时 `.env` 中未配置 DeepSeek、OpenAI 或通用 LLM API Key。
- 当前 AI 实际处于“本地演示模式”；即使配置了本机 Ollama/LM Studio 等端口，现有代码具备自动探测逻辑。
- `/api/ai/config` 只公开 provider、label、model、configured，不公开 Key。

### 当前工作流

```text
负责人开启 AI
  -> 点击“开始思考”
  -> generateDraft
  -> generateDirections
  -> 状态 voting
  -> 成员匿名投票
  -> 全部成员投完或负责人结束
  -> 公告
  -> 3 轮以上可进行深度分工
  -> generateDeepPlan
  -> 每位成员独立读取自己的任务
```

### 已具备的 AI 安全措施

- 聊天内容用 `<user_data>` 包装，并带不可信数据提示。
- AI 输出控制字符被清理，长度和数组数量有限制。
- AI 调用有全局并发限制和按话题频率限制。
- AI 进度写入话题状态，通过 SSE 触发刷新。
- AI 输出不作为 HTML 直接插入；前端通过 `escapeHtml()` 转义。

### 主要问题

- 输出只用“去掉 ```、找第一个 `{` 和最后一个 `}`、`JSON.parse`”的宽松解析，没有 JSON Schema 校验。
- `generateDeepPlan` 中的公告、草稿、成员列表没有全部用 `<user_data>` 包装，存在提示注入传播链。
- 没有结构化输出的 provider 原生能力，没有有限重试，没有 Prompt 版本号和评测集。
- 没有清晰的 `ANALYZE -> CLARIFY -> RESEARCH -> GENERATE -> EVALUATE -> RECOMMEND -> WAIT_FOR_LEADER -> DECOMPOSE -> ASSIGN` 状态机。
- 目前只有按话题限流，没有按用户、按账号、按天或全站预算；用户可以创建多个项目绕过单项目 AI 限额。
- `AI_TIMEOUT_MS` 环境变量没有真正控制 `callLLM()`，后者硬编码 60 秒。
- 公开话题 API 会暴露 `topic.ai`，这比普通 AI 功能问题更严重，已列为 Critical。

## 10. Render / Docker / 数据持久化

### Render 当前配置

- `render.yaml` 使用 Render Node Web Service。
- `plan: free`，闲置约 15 分钟休眠。
- `data/` 是 Web Service 容器内的本地目录。
- `disk` 配置被注释，意味着没有独立的持久磁盘。
- `ADMIN_PASSWORD` 使用 `sync: false`，没有写入 Git。
- `ALLOWED_ORIGINS` 为空，默认不开放跨域，这是安全的正向配置。

### 数据丢失原因

Render 免费 Web Service 的文件系统是临时文件系统，和应用实例生命周期绑定：

```text
应用实例创建
  -> server.js 创建 data/store.json
  -> 用户产生数据
  -> 服务休眠/重启/重新部署/换实例
  -> 新实例文件系统重新开始
  -> data/ 不存在或变成空数据
```

`Dockerfile` 声明了 `VOLUME ["/app/data"]`，只有真正挂载持久卷时才有意义。当前 Render 蓝图没有启用 `disk`，因此 Docker 的 VOLUME 不会自动解决 Render 托管 Node 服务的数据持久化问题。

### 当前备份问题

- `store.json.bak` 和每日快照都位于 `data/` 内。
- 一旦实例被替换，主文件和备份一起消失。
- 没有外部对象存储、数据库备份或可独立恢复的快照。
- 没有自动化恢复演练和备份完整性校验。

## 11. 风险分级

## Critical

### C-01 Render 临时文件系统和 JSON 数据存储绑定，可能整站数据丢失

- 文件：`render.yaml`、`server.js`
- 位置：`render.yaml:24-29`、`server.js:37-40`、`server.js:105-170`
- 当前证据：Render free 实例没有挂载持久磁盘；数据、Session、消息、文件和备份全在 `data/`。
- 攻击方式：不一定是攻击；正常休眠、重启、重新部署或实例替换即可触发。
- 影响：用户、项目、申请、聊天、文件元数据、AI 状态和通知全部可能丢失；恢复只能依赖仍存在的旧实例文件。
- 修复方案：Phase 1 迁移到独立持久化存储。优先评估 Render PostgreSQL；迁移前导出 `store.json` 和文件元数据，提供回滚脚本。文件和数据库必须分别备份，不能只备份 Web Service 目录。
- 测试方案：创建用户、话题、消息、申请、通知和文件 -> 重启/模拟新实例 -> 验证全部存在；执行一次完整备份和恢复演练。

### C-02 公开话题接口泄露 AI 草稿、投票和深度分工内容

- 文件：`server.js`
- 位置：`server.js:453-459`、`server.js:718-734`
- 当前逻辑：`publicTopic()` 只浅拷贝 `topic` 并删除密码字段，没有删除 `ai`；`viewTopic()` 随后直接返回该副本。
- 攻击方式：未登录用户请求 `GET /api/topics` 或 `GET /api/topics/{id}`。只要话题存在，响应中的 `ai` 可能包含 `draft`、`options`、`announcement`、`deep.items`。
- 影响：私密话题的 AI 草稿、候选方案、投票内容、成员任务、成员 ID、书目和个人建议可能泄露；即使非成员不能进入聊天，也能读取 AI 规划内容。
- 修复方案：话题公开响应必须使用白名单投影；只返回 `aiEnabled`、`aiRounds` 等必要摘要。完整 AI 数据只允许通过已鉴权的 `/api/topics/{id}/ai` 返回，并按成员/负责人分别投影。
- 测试方案：创建私密话题并写入深度分工 -> 未登录请求列表和详情 -> 断言响应不存在 `ai`、`draft`、`options`、`announcement`、`deep`、`items`、成员任务和建议。

### C-03 历史管理员凭据已进入 Git 历史，当前管理员凭据必须视为已泄露

- 文件：历史提交 `5b61ad64a7cfc8d174e7efbf56e7bdb3bb5ff9ac`、`cc6958491580f5a2f466494bff11717719c909aa` 中的 `server.js` / `README.md`
- 位置：历史版本的 ADMIN_PASSWORD 初始化逻辑和文档
- 当前状态：当前源代码已经改为只从环境变量读取，且 `ensureAdmin()` 已不再每次启动无条件重置密码；但历史凭据仍在仓库历史中，且当前 `.env` 已配置 `ADMIN_PASSWORD`。
- 攻击方式：任何取得历史 ZIP、README 或 Git 历史的人都可以尝试旧管理员凭据；旧凭据也可能被用于登录仍然使用旧哈希的生产管理员账号。
- 影响：管理员接管、任意删除项目、封禁账号、访问全部聊天和文件。
- 修复方案：立即显式执行一次管理员密码轮换并删除所有旧 Session；轮换后确认旧凭据失败、新凭据成功。不要尝试“删除 Git 历史里的字符串”来代替轮换。生产 Secret 只放在 Render/云 Secret 管理中。
- 测试方案：轮换前记录旧 Session；轮换后验证旧 Session 被清除、旧密码失败、新密码成功、日志不输出密码。当前审计未读取或输出任何密码。

## High

### H-01 负责人/管理员可以撤回其他成员消息

- 文件：`server.js`、`script.js`
- 位置：`server.js:1378-1391`、`script.js:895-907`
- 当前逻辑：`const mine = msg.author === user.id; if (!mine && !isOwner(topic, user)) ...`
- 影响：负责人或管理员可以篡改其他成员表达记录，和最终验收“用户 A 无法撤回用户 B 消息”冲突。
- 修复方案：普通撤回必须严格 `msg.author === user.id`；如果管理员确实需要内容治理，另设独立的 moderation 操作、审计原因和不同 UI，不混用“撤回”。
- 测试方案：成员 A 发消息 -> 负责人调用 DELETE -> 返回 403；A 自己撤回 -> 成功；管理员走独立管理接口才可处理。

### H-02 私密话题 6 位密码缺少专用暴力破解防护

- 文件：`server.js`、`security.js`
- 位置：`server.js:1262-1274`、`security.js:17-52`
- 当前只有全站 API 每 IP 限流，没有按 `topicId + userId`、`topicId + IP` 的密码失败锁定。
- 影响：6 位纯数字空间只有 100 万，攻击者可通过代理或轮换账号进行离线式在线爆破，并可能绕过 IP 总量限制。
- 修复方案：专用验证限流、指数退避、失败次数锁定、审计告警；必要时限制申请频率并要求负责人确认。
- 测试方案：连续错误密码达到阈值后锁定；正确密码在锁定期内也拒绝；锁定过期后可恢复。

### H-03 AI 输出缺少严格 Schema、有限重试和 Prompt 版本管理

- 文件：`ai.js`、`server.js`
- 位置：`ai.js:155-169`、`ai.js:340-426`、`server.js:1150-1164`
- 当前只用宽松 JSON 截取和少量 shape 检查；模型返回文本字段后由服务端截断，未做严格类型、长度、枚举和语义校验。
- `generateDeepPlan` 中公告和草稿未全部按不可信数据包装，存在聊天内容经上一轮输出再次进入下一轮上下文的风险。
- 影响：无效或受污染输出、错误分工、信息泄漏、模型行为不可重复，无法做可靠回归测试。
- 修复方案：定义 JSON Schema，按 provider 使用结构化输出，至少有限重试；Prompt 加版本号；所有用户派生文本统一包装并做二次清洗；建立固定评测样本。
- 测试方案：非法 JSON、缺字段、类型错误、超长文本、提示注入样本和正常样本全部覆盖。

### H-04 AI 调用缺少用户/全站预算，可通过创建多个项目放大费用

- 文件：`server.js`、`ai.js`
- 位置：`server.js:1127-1177`、`server.js:1214-1257`
- 当前只有 `aiPerTopic` 限流和全局并发闸门，没有按用户、按天、按 provider 或全站成本预算。
- 影响：一个用户可以创建多个项目反复触发模型或联网检索，造成 API 费用或外部服务消耗；即使没有 API Key，本地模型也可被反复占用。
- 修复方案：增加用户级/全站级窗口和日预算、AI 请求审计、可配置禁用开关和紧急熔断。
- 测试方案：同一用户跨多个话题触发调用 -> 达到用户预算后 429；全局达到预算后拒绝并记录。

### H-05 JSON 落盘窗口、异常吞掉和无优雅关闭会造成数据丢失

- 文件：`server.js`
- 位置：`server.js:131-170`
- 消息发送和通知读取状态使用 `scheduleSave()`；写入失败只 `console.error`，接口仍可能返回成功；没有 SIGTERM/SIGINT 强制 flush。
- 影响：Render 部署、进程崩溃、磁盘满或权限错误时，最近消息、通知状态和成员变更可能丢失，用户却收到成功响应。
- 修复方案：迁移到具备事务和恢复能力的持久化层；过渡期增加写入结果检查、同步刷盘、优雅关闭和写入失败向 API 返回错误。
- 测试方案：模拟磁盘满、进程终止、频繁写入和多实例并发，验证无静默丢失。

### H-06 没有自动化测试和恢复测试

- 文件：`package.json`、仓库根目录
- 位置：`package.json` 仅定义 `start`
- 影响：权限、申请、SSE、AI 和数据迁移修改没有回归保护。
- 修复方案：Phase 8 建立 API、权限、数据库、聊天、申请、通知、AI、移动端和安全测试；恢复测试必须在隔离环境执行。
- 测试方案：所有修复对应一个自动化断言，并纳入 CI/发布前检查。

### H-07 文件存储缺少全站配额、异步 I/O 和内容安全扫描

- 文件：`server.js`
- 位置：`server.js:1407-1471`
- 当前有单文件 5MB、单话题数量、单用户 50MB 和魔数校验，但没有全站存储配额、杀毒/恶意文档扫描、异步写入和孤儿文件回收。
- 影响：攻击者可通过大量账号或轮换 IP 消耗磁盘；恶意 Word 文件可能被其他成员下载。
- 修复方案：增加全站容量和配额、异步流式写入、文件状态/扫描任务、失败清理和孤儿回收。
- 测试方案：磁盘不足、并发上传、伪装扩展名、超配额、删除后清理和文件恢复。

## Medium

### M-01 聊天时间使用服务端本地字符串，和统一时间策略不一致

- 文件：`server.js`、`script.js`
- 位置：`server.js:1590`、`script.js:891`、`script.js:903`
- 影响：Render 通常运行 UTC，用户看到的时间可能相差 8 小时，且和申请/通知显示规则不同。
- 修复方案：服务端只返回 epoch/ISO，客户端使用 `Intl.DateTimeFormat` 或统一 `formatDate(at)`。
- 测试方案：在 UTC、Asia/Shanghai、America/New_York 浏览器时区验证同一消息。

### M-02 限流、登录失败计数和 Session 都在单进程内存/JSON 中

- 文件：`security.js`、`server.js`
- 位置：`security.js:168-193`、`server.js:94-99`
- 影响：重启后限流重置；多实例部署时各实例状态不共享，攻击者可以绕过；登录失败 Map 没有 TTL 清扫。
- 修复方案：正式部署使用共享缓存/数据库；至少给 Map 加 TTL 清理并明确只支持单实例。
- 测试方案：重启、多实例、并发请求和长期运行内存检查。

### M-03 Session token 明文保存在 JSON 中

- 文件：`server.js`
- 位置：`server.js:225-242`
- 影响：`store.json` 泄露或备份外泄时，攻击者可直接重放有效 Session。
- 修复方案：存储 token 的哈希而不是原文；支持逐设备撤销、定期轮换；备份中不导出活跃 Session。
- 测试方案：备份泄露模拟、Session 撤销、过期清理和重放测试。

### M-04 CSRF 依赖未签名双提交 Cookie，且同源判断信任 forwarded host

- 文件：`server.js`、`security.js`
- 位置：`server.js:400-415`、`server.js:755-764`、`security.js:58-64`
- 影响：在错误的代理配置或共享父域环境下，攻击面会扩大；`X-Forwarded-Host` 不应无条件参与同源判断。
- 修复方案：使用签名 CSRF token，严格校验 Origin/Referer 和受信任代理链；只允许明确配置的代理头。
- 测试方案：伪造 Origin、修改 X-Forwarded-Host、缺少/错误 token、跨站 POST。

### M-05 私密话题的简介、氛围和角色需求仍可能在列表/详情返回

- 文件：`server.js`
- 位置：`server.js:718-734`
- 当前私密话题只隐藏 members 和项目编号，代码注释声称隐藏“简介细节”，但 desc、vibe、neededRoles 等字段仍可能返回。
- 影响：未加入成员或未登录用户可能看到比设计预期更多的项目内容。
- 修复方案：按访问者角色建立明确字段投影表。
- 测试方案：游客、非成员、成员、负责人、管理员五种视角快照对比。

### M-06 上传采用 base64 + 同步写文件，可能阻塞事件循环

- 文件：`server.js`
- 位置：`server.js:1422-1452`
- 影响：同时上传 5MB 文件时，JSON 解析、Buffer 解码和 `writeFileSync` 会阻塞其他用户请求。
- 修复方案：改为 multipart/流式上传、异步临时文件写入、原子提交和失败清理。
- 测试方案：并发上传和大文件压力测试。

### M-07 话题和消息数据没有总量上限或归档策略

- 文件：`server.js`
- 位置：`server.js:923-973`、`server.js:1308-1375`
- 影响：所有话题、消息和文件元数据常驻内存并整体写入 JSON；用户量和数据量增长后会造成内存、写放大和响应体过大。
- 修复方案：迁移数据库、分页、归档、消息保留策略和索引。
- 测试方案：大批量数据、分页边界、长消息历史。

### M-08 没有数据库 schema 版本和正式迁移机制

- 文件：`server.js`
- 位置：`server.js:101-129`
- 当前只对缺字段做兼容默认值和私密密码惰性迁移，不能安全演进字段。
- 修复方案：迁移表、版本号、升级前备份、回滚和 dry-run。
- 测试方案：从每个历史版本样本升级到最新 schema。

### M-09 健康检查只验证进程活着，不验证数据可写和依赖可用

- 文件：`server.js`
- 位置：`server.js:780-785`
- 修复方案：增加数据写入/数据库连接健康检查，区分 liveness 和 readiness。
- 测试方案：只读磁盘、数据库断开、AI 可选依赖不可用时验证状态。

### M-10 移动端尚未完成真实浏览器回归

- 文件：`styles.css`
- 位置：`styles.css:160`、`styles.css:248-285`、`styles.css:423-456`
- 修复方案：用 320/375/390/430/768 宽度逐页回归，增加 `dvh`、safe-area、软键盘可视区域处理。
- 测试方案：截图和横向滚动检测，手动测试聊天输入框、弹窗、通知、文件列表和 AI 折叠面板。

### M-11 AI 超时配置与实际调用不一致

- 文件：`security.js`、`ai.js`
- 位置：`security.js:17-22`、`ai.js:99-136`
- `AI_TIMEOUT_MS` 已定义，但 `callLLM()` 硬编码 60 秒；Tavily 联网调用也没有使用同一预算。
- 修复方案：统一从配置读取超时，限制搜索和模型的组合耗时。

### M-12 README 存在与当前代码不一致的安全说明

- 文件：`README.md`
- 位置：`README.md:257-259`
- 文案仍提到“管理员账号默认密码写在代码里作为兜底”，当前代码已经没有真实默认密码。
- 这会误导部署者，也会让人误以为需要保留默认凭据。
- 修复方案：更新安全说明、持久化限制和恢复流程；不得写真实管理员凭据。

## Low

### L-01 `package-lock.json` 未跟踪

- 文件：`package-lock.json`
- 当前项目无第三方运行时依赖，风险暂时较低，但构建可重复性和供应链审计仍应纳入版本控制。

### L-02 登录不存在账号时跳过 scrypt，存在时序差异

- 位置：`server.js:864-873`
- 可能被用于昵称枚举，但当前登录限流已经降低价值；可增加等时 dummy hash。

### L-03 审计日志轮转文件没有清理策略

- 文件：`security.js`
- 位置：`security.js:298-330`
- 每次超过 5MB 会生成新的 `.bak`，长期运行可能积累文件。

### L-04 `README`/部署文档对 Render 免费实例的休眠和临时磁盘提示不够醒目

- 位置：`render.yaml:24-29`、`README.md:156-172`
- 部署者可能误以为本地 `data/` 会自动持久化。

### L-05 昵称只有长度和唯一性校验，没有 Unicode 混淆和仿冒治理

- 位置：`server.js:820-825`
- 可能造成视觉仿冒，属于低优先级产品安全项。

## 12. 已经存在的安全良好实践

以下措施是当前代码中已经存在的，修复阶段不应无故删除：

- 密码使用 `crypto.scryptSync` 加盐哈希，没有明文保存普通用户密码。
- `ADMIN_PASSWORD` 已改为只从环境变量读取，当前启动逻辑不会每次重启重置已有管理员。
- 管理员初始化失败时会明确拒绝启动，而不是使用硬编码弱密码。
- Session 使用随机 token，浏览器端使用 HttpOnly Cookie。
- 提供 CSRF 双提交校验，写操作必须带匹配的 CSRF Header。
- SSE 使用一次性短期 ticket，不再把长期 Session token 放进 URL。
- 静态文件使用白名单，不直接暴露 `data/`、`.env`、日志和后端源码。
- 输出 JSON 使用 no-store，并统一添加 CSP、X-Content-Type-Options、Referrer-Policy、X-Frame-Options、Permissions-Policy、CORP 等响应头。
- 请求体有大小、JSON 深度、数组长度、对象字段数和字符串长度限制。
- 有登录/注册/API/发言/上传/AI 限流，有全局和单 IP 并发闸门。
- SSE 有全局、单 IP 和空闲连接限制。
- 上传有扩展名白名单、大小限制、魔数校验、生成文件名和单话题/单用户配额。
- 删除文件和删除话题都会尝试删除对应物理文件。
- 数据写入使用临时文件 + rename，并保留 `.bak` 和每日快照；损坏 JSON 会保留现场并拒绝启动，不静默清空。
- 审计日志字段采用白名单，不记录密码、Session token 和 API Key。
- 前端动态渲染大多使用 `escapeHtml()`，未发现直接的动态 HTML 拼接执行入口。
- Docker Compose 使用只读根文件系统、临时 `/tmp`、非 root 用户、`cap_drop: ALL` 和 `no-new-privileges`。
- CORS 默认关闭，只有显式白名单 Origin 才回显。
- 当前无 SQL 数据库和命令行拼接，未发现 SQL 注入或命令注入入口；也没有将用户可控 URL 传给服务器请求的 SSRF 入口。

## 13. Phase 1～Phase 10 整改路线图

### Phase 1：数据持久化与恢复

- 先备份 Git、`store.json`、`data/files/` 和当前 `.env` 的 Secret 配置到独立安全位置。
- 选择 Render PostgreSQL 或其他托管持久数据库；先写迁移方案、字段映射、双写/影子读方案和回滚流程。
- 迁移时保留现有用户、项目、消息、申请、通知、举报和文件元数据。
- 建立版本化迁移、备份、恢复和定期恢复演练。
- 验收：创建用户/项目/消息/申请/通知 -> 重启/模拟新实例 -> 数据完整；完成一次恢复演练。

### Phase 2：权限和业务逻辑

- 修复 AI 字段投影泄露。
- 将消息撤回严格限定为作者本人；管理员治理使用独立接口。
- 复核所有 IDOR、封禁状态、管理员接口和文件/聊天访问。
- 为每个权限规则建立 API 级回归测试。

### Phase 3：项目申请

- 统一 PENDING/ACCEPTED/REJECTED/CANCELLED 状态。
- 增加取消、重复申请、拒绝后冷却、频率限制和人数上限回归。
- 前后端都显示同一服务端状态。

### Phase 4：移动端 UI 与时间

- 统一 epoch/ISO 存储和客户端时区格式化。
- 在 320/375/390/430/768px 完成真实浏览器回归。
- 处理软键盘、安全区、横向溢出、弹窗和触控尺寸。

### Phase 5：聊天与通知

- 重做消息状态、编辑/复制/引用/撤回权限分离。
- 统一通知类型、未读/已读、全部已读、跳转和通知与聊天分离。
- 增加消息发送失败重试和并发冲突策略。

### Phase 6：AI 工作流

- 实现有限状态机、结构化 Schema、超时/重试、Prompt 版本和评测集。
- 只有负责人确认后进入任务拆解。
- 增加按用户、按项目、按天和全站 AI 预算。
- 所有用户派生内容统一作为不可信数据输入。

### Phase 7：全面安全加固

- 覆盖 RBAC、IDOR、XSS、CSRF、Session、CORS、上传、路径、SSRF、限流、SSE、AI Prompt Injection、敏感信息和日志。
- 每个修复都添加回归测试。

### Phase 8：自动化测试

- Unit / Integration / API / Permission / Database / Chat / Application / Notification / AI / Mobile / Security / Recovery。
- 恢复测试必须在隔离环境执行，不得操作生产数据。

### Phase 9：Render 正式部署

- 数据库与 Web Service 生命周期分离。
- 使用正式 Secret 管理，生产禁止 Debug。
- 部署前备份，部署后冒烟测试、数据完整性检查、回滚验证。

### Phase 10：最终验收

- 重启后数据仍在。
- 用户 A 无法撤回用户 B 消息。
- 伪造 ID 无法越权。
- 普通用户无法访问管理员 API。
- 重复申请、人数上限、封禁状态正确。
- 390px 核心页面无横向溢出。
- 不同时区时间正确。
- 通知未读/已读正确。
- AI 按定义状态机、Schema 和负责人确认流程运行。

## 14. Phase 0 结论与进入条件

Phase 0 已完成，未修改业务代码。当前最高优先级不是继续加功能，而是：

1. 立即把历史管理员凭据视为已泄露并进行显式轮换。
2. 在任何代码重构前备份 Git 和当前数据。
3. 解决 Render 临时文件系统，建立独立持久化和外部可恢复备份。
4. 修复 AI 字段公开泄露和消息撤回越权。
5. 建立自动化权限和恢复测试，再进入 UI、通知和 AI 工作流重构。

在 Phase 1 开始前，必须先确认目标持久化方案（Render PostgreSQL 或其他托管持久存储）并准备可回滚的数据迁移；不得直接删除现有 `data/` 或生产数据。
