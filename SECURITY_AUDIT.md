# ProjectHub V2 安全审计

## 本轮已修复

- 删除公开话题响应中的完整 `ai` 对象，防止草稿、投票、公告和深度分工泄露。
- 私密话题对非成员隐藏简介、氛围和加入方向要求。
- 消息撤回严格限制为消息作者本人。
- 生产环境无 PostgreSQL 时拒绝启动，禁止默认落到临时文件系统。
- Session token 改为 SHA-256 哈希后存储；旧明文 Session 启动时自动迁移。
- PostgreSQL 状态写入增加 revision 乐观锁，过期实例不能覆盖新版本数据。
- CSRF Token 改为 HMAC 签名并与当前用户绑定。
- 关键限流迁移到 PostgreSQL 共享计数，多实例部署下不会各自绕过。
- API 请求体拒绝数组根节点。
- 申请增加单用户限流、取消申请、拒绝/取消后的重新申请冷却。
- 文件增加全站总容量上限。
- AI 增加单用户和全站每日调用预算。
- AI 增加 Prompt 版本、有限重试和输出结构校验。
- 健康检查会验证存储依赖。

## 当前仍存在的风险

- PostgreSQL 适配器已通过 mock 单元测试，但尚未在真实 PostgreSQL 上执行集成测试。
- 真实文件仍以 base64 JSON 上传后写入存储，尚未改为 multipart 流式上传。
- 没有外部杀毒/恶意文档扫描。
- CSRF 仍是双提交 Cookie，不是签名 Token。
- 限流和登录失败计数仍是单实例内存状态，多实例部署需要共享缓存。
- 还未实现完整的外层 HTTPS、邮件验证、验证码和账户找回流程。
- 旧管理员凭据一旦曾经外发，必须显式轮换；本轮没有在日志或报告中输出任何密码。

## 上线前必须完成

1. 在真实 PostgreSQL 测试环境完成迁移和恢复。
2. 轮换历史管理员密码并验证旧 Session 失效。
3. 在 320/375/390/430 宽度完成移动端浏览器回归。
4. 用生产域名检查 HTTPS、Cookie Secure、Origin 和 CORS。
5. 配置数据库和文件备份的异地保存。
